#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
	unsigned long long n = 0;
	cin >> n;
	
	cout << n * (n - 1) * (n - 2) / 6 << endl << 3;
	

	return 0;
}